services:
  - type: web
    name: leadway-cfo-dashboard
    runtime: static
    buildCommand: npm install && node ./node_modules/vite/bin/vite.js build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
