[build]
command = "npm install --include=dev && npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "20"
NODE_ENV = "production"
NPM_FLAGS = "--include=dev"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
